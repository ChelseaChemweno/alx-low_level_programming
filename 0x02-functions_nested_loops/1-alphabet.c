#include "main"
/**
 *print_alphabet -> prints the lowercase alphabets 
 *
 *Return: always 0 (success)
 */

void print_alphabet(void);

{
 int p;
 for (p = 'a' ; p <= 'z' ; p++)
 {
 _putchar (p);
 }
 _putchar('\n')

	 return (0);
}
